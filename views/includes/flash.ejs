   <!-- <%if(success && success.length) {%> -->
    <div class="flex  items-center justify-center mt-2">
    <div id="flashMessage  " class="bg-green-100 border border-green-400 text-green-700 px-4 py-2 rounded relative " role="alert">
        <strong class="font-bold"></strong>
        <span class="block sm:inline mr-12"><%= success%></span>
        <button onclick="this.parentNode.style.display = 'none';" class="absolute top-0 right-0 px-4 py-2 ">
            <i class="fa-solid fa-square-xmark fa-xl"></i>
        </button>
    </div>
</div>
     <!-- <%} %>  -->

      <!-- <%if(error && error.length) {%> -->
    <div class="flex  items-center justify-center mt-2">
        <div id="flashMessage  " class="bg-red-100 border border-red-400 text-red-700 px-4 py-2 rounded relative " role="alert">
            <strong class="font-bold"></strong>
            <span class="block sm:inline mr-12"><%= error%></span>
            <button onclick="this.parentNode.style.display = 'none';" class="absolute top-0 right-0 px-4 py-2 ">
                <i class="fa-solid fa-square-xmark fa-xl"></i>
            </button>
        </div>
    </div>
         <!-- <%} %>  -->
